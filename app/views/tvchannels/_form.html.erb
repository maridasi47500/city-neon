<%= form_with(model: tvchannel) do |form| %>
  <% if tvchannel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tvchannel.errors.count, "error") %> prohibited this tvchannel from being saved:</h2>

      <ul>
        <% tvchannel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: radiowafe) do |form| %>
  <% if radiowafe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(radiowafe.errors.count, "error") %> prohibited this radiowafe from being saved:</h2>

      <ul>
        <% radiowafe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
